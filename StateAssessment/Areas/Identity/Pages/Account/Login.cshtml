@page
@model LoginModel

@{
    ViewData["Title"] = "Log in";
}
@{
    Layout = "/Areas/Identity/Pages/_Layout.cshtml";
}
<div id="login_page">
<div class="image-element" style="z-index:0;"></div>
<div class="container">
    <div id="head-logo">
        <a href="/"><img src="~/images/logo.png"></a>
    </div>
    <div class="row align-items-center">
        <div class="col login-text" style="z-index:1;">
            <h1 class="mb-3">700+ employees throughout the U.S., Puerto Rico and Canada</h1>
            <p>Per our discussion, this personality assessment is the next step in our interview process. There is no need to study or prepare for it, however please be sure to have the time available and a comfortable setting so you are able to concentrate. It is recommended that you set aside at least an hour to complete all 6 sections.</p>
        </div>
        <div class="col form-col">
            <div class="form-col-in">
                <h1>Welcome!</h1>
                <p class="mb-4">Thank you for your interest in a career with State Industrial Products</p>
                <form id="account" action="" method="post" id="login_form">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="mb-3">
                        <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" />
                        @*<label asp-for="Input.Email" class="form-label"></label>*@
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>
                        <div class="mb-2">
                            <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true"   />
                        <i class="fa fa-eye-slash" aria-hidden="true" id="togglePassword"></i>
                        @*<label asp-for="Input.Password" class="form-label"></label>*@
                        <span asp-validation-for="Input.Password" class="text-danger"></span>
                    </div>
                   
                    <div class="mb-3 reset-pwd">
                        <a id="forgot-password" asp-page="./ForgotPassword">Reset Password</a>
                        <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Register as a new user</a>
                        <a id="resend-confirmation" asp-page="./ResendEmailConfirmation">Resend email confirmation</a>
                    </div>
                    <div class="mb-4">
                        <button type="submit" class="btn btn-primary" id="login_btn">Sign in</button>
                    </div>
                </form>
                <p class="note"><b>Note:</b> If you experience any difficulty logging into the testing system, please contact the recruiting department.</p>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
<script src="https://code.jquery.com/jquery-3.6.1.min.js" integrity="sha256-o88AwQnZB+VDvE9tvIXrMQaPlFFSUTR+nldQm1LuPXQ=" crossorigin="anonymous"></script>

<script>
    const togglePassword = document
        .querySelector('#togglePassword');

    const password = document.querySelector('#Input_Password');

    jQuery(togglePassword).click(function () {
        const type = password
            .getAttribute('type') === 'password' ?
            'text' : 'password';

        password.setAttribute('type', type);

        // Toggle the eye and bi-eye icon
        jQuery(this).toggleClass('fa-eye');
    });
</script>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
